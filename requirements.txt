# Core deep learning stack
# For CUDA wheels, install torch/torchvision from the official PyTorch index
# matching your local CUDA version, e.g.:
#   pip install torch==2.1.0+cu121 torchvision==0.16.0+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.1.0+cu121
torchvision==0.16.0+cu121
xformers==0.0.22.post7

# Stable Diffusion / Hugging Face runtime
accelerate==1.2.1
diffusers==0.35.2
huggingface-hub==0.36.0
packaging==25.0
peft==0.17.0
requests==2.32.3
safetensors==0.4.5
tokenizers==0.21.4
transformers==4.47.0

# 3D Gaussian Splatting dependencies
numpy==1.26.4
opencv-python==4.9.0.80
pillow==10.4.0
plyfile==1.0.3
tqdm==4.67.1

# Optional logging used by train_style.py when available
tensorboard==2.18.0

# Required 3DGS CUDA extensions. Install these from the Gaussian Splatting
# submodules in your local checkout; the old absolute /nfsshare paths were
# environment-specific and intentionally removed.
# diff_gaussian_rasterization
# fused_ssim
# simple_knn
