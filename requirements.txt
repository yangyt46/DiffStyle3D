# Recommended environment:
#   Python 3.10
#   NVIDIA GPU + recent NVIDIA driver
#   CUDA runtime wheel: cu121
#
# Install:
#   pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu121

# Core deep learning stack
torch==2.1.0+cu121
torchvision==0.16.0+cu121
xformers==0.0.22.post7

# Stable Diffusion / Hugging Face runtime
accelerate==1.2.1
diffusers==0.35.2
huggingface-hub==0.36.0
packaging==25.0
peft==0.17.0
requests==2.32.3
safetensors==0.4.5
tokenizers==0.21.4
transformers==4.47.0

# 3D Gaussian Splatting dependencies
numpy==1.26.4
opencv-python==4.9.0.80
pillow==10.4.0
plyfile==1.0.3
tqdm==4.67.1

# Optional logging used by train_style.py
tensorboard==2.18.0

# Local CUDA extensions.
# These cannot be installed from PyPI unless your repo includes them as local submodules.
# If your project has these folders, uncomment the relative paths below:
#
# ./submodules/diff-gaussian-rasterization
# ./submodules/fused-ssim
# ./submodules/simple-knn